#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a==b)
    {
        printf("the two integers are equal");
    }
    else
    { 
        printf("the two integers are no");
    }
    return 0;

}